class AppVectors {
  static const vectorPath = 'assets/vectors/';
  static const format = '.svg';
  static const spotifyLogo = '${vectorPath}spotify_logo$format';
  static const moonIcon = '${vectorPath}moon$format';
  static const sunIcon = '${vectorPath}sun$format';
  static const topPatternPic = '${vectorPath}top_pattern$format';
  static const bottomPatternPic = '${vectorPath}bottom_pattern$format';
}